{
  "name": "@quantakrypto/action",
  "version": "0.1.0",
  "description": "quantakrypto Action — fail CI when new quantum-vulnerable cryptography lands. GitHub Action wrapping qScan.",
  "license": "Apache-2.0",
  "author": "Dandelion Labs <hello@dandelionlabs.io> (https://dandelionlabs.io)",
  "homepage": "https://github.com/quantakrypto/tools/tree/main/packages/action#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/quantakrypto/tools.git",
    "directory": "packages/action"
  },
  "bugs": {
    "url": "https://github.com/quantakrypto/tools/issues"
  },
  "type": "module",
  "private": true,
  "main": "./dist/main.js",
  "files": [
    "dist",
    "action.yml",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@quantakrypto/core": "0.1.0",
    "@quantakrypto/qscan": "0.1.0"
  },
  "scripts": {
    "build": "tsc -b",
    "bundle": "esbuild src/main.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.js --banner:js=\"import { createRequire as __cr } from 'module'; const require = __cr(import.meta.url);\"",
    "test": "node --import tsx --test test/*.test.ts"
  }
}
